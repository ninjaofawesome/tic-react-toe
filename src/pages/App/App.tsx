import React from 'react';

// components
import Table from '../../components/Table/Table';
import { AppWrapper } from './AppStyles';

const App: React.FC = () => {
  return (
    <AppWrapper>
      <Table />
    </AppWrapper>
  );
}

export default App;
